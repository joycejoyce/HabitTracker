"use strict";

System.register([], function (_export, _context) {
  "use strict";

  var HTML_CLASS, HTML_ID, HTML_PROPERTY, CSS_PROPERTY, CSS_VALUE, EVENT;
  return {
    setters: [],
    execute: function () {
      _export("HTML_CLASS", HTML_CLASS = {
        queryForm: "query-form"
      });

      _export("HTML_ID", HTML_ID = {
        createTrackTblBtn: "create-track-tbl-btn",
        result: "result",
        queryTableTrack: "query-table-track",
        trackTableQueryForm: "track-table-query-form"
      });

      _export("HTML_PROPERTY", HTML_PROPERTY = {
        id: "id"
      });

      _export("CSS_PROPERTY", CSS_PROPERTY = {
        display: "display"
      });

      _export("CSS_VALUE", CSS_VALUE = {
        none: "none",
        block: "block"
      });

      _export("EVENT", EVENT = {
        click: "click",
        submit: "submit"
      });
    }
  };
});