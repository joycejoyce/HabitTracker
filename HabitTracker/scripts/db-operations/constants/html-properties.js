const HTML_CLASS = {
    queryForm: "query-form"
}

const HTML_ID = {
    createTrackTblBtn: "create-track-tbl-btn",
    result: "result"
};

const HTML_PROPERTY = {
    id: "id"
};

const CSS_PROPERTY = {
    display: "display"
};

const CSS_VALUE = {
    none: "none",
    block: "block"
}

const EVENT = {
    click: "click",
    submit: "submit"
};

export {
    HTML_CLASS,    
    HTML_ID,
    HTML_PROPERTY,
    CSS_PROPERTY,
    CSS_VALUE,
    EVENT
};