const HTML_ID = {
    createTrackTblBtn: "create-track-tbl-btn",
    textArea: "text-area"
};

const HTML_PROPERTY = {
    id: "id"
};

const EVENT = {
    click: "click"
};

export {
    HTML_ID,
    HTML_PROPERTY,
    EVENT
};